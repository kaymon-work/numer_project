server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # รองรับ SPA: ถ้าไม่พบไฟล์จริง ให้ fallback ไป index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
